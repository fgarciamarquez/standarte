; Límites de subida para la zona privada de proyectos (Storage llega a 100 MB).
; OVH aplica .user.ini por carpeta (PHP-FPM/CGI). Afecta a /admin y subcarpetas.
upload_max_filesize = 120M
post_max_size = 121M
max_execution_time = 300
max_input_time = 300
memory_limit = 256M
