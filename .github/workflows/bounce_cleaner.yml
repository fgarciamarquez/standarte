name: Automated Bounce Email Cleaner

on:
  schedule:
    # Se ejecuta todos los días a las 03:00 UTC (4:00 AM en España) - Limpieza diaria de mantenimiento
    - cron: '0 3 * * *'
    # 5 minutos después de cada envío drip (8:05 a 18:05 UTC), de lunes a viernes (alineado con el drip)
    - cron: '5 8-18 * * 1-5'
  workflow_dispatch: # Permite ejecutarlo manualmente desde GitHub Actions

jobs:
  clean-bounces:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Bounce Handler Cron URL
        run: |
          curl -sS "https://standarte.es/bounce-handler.php?cron=1&token=b356d78a8f192b0c95098e94a861d8a2"
